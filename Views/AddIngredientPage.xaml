<Page
    x:Class="RecipesWinUI.Views.AddIngredientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:RecipesWinUI.Converters" 
    xmlns:controls="using:RecipesWinUI.Controls">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
    </Page.Resources>

    <Grid>

        <ScrollViewer Background="#FFE8D8B8"
                      VerticalScrollBarVisibility="Auto">

            <Grid HorizontalAlignment="Center"
                  Padding="24">

                <Border Background="#FFF5E4C1"
                        BorderBrush="#FFD7C0A1"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="24"
                        Width="500"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                    <StackPanel Spacing="20">

                        <TextBlock Text="Összetevő hozzáadása"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="#FF4B3A2E"/>

                        <StackPanel>
                            <TextBlock Text="Név"
                                       FontWeight="SemiBold"
                                       Foreground="#FF4B3A2E"/>

                            <TextBox Text="{Binding EditedName, Mode=TwoWay}"
                                     PlaceholderText="pl. Alma"
                                     FontSize="18"
                                     BorderThickness="0"
                                     Padding="12,6"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Kalória / 100g"
                                       FontWeight="SemiBold"
                                       Foreground="#FF4B3A2E"/>

                            <TextBox Text="{Binding EditedCalorie, Mode=TwoWay}"
                                     PlaceholderText="pl. 52"
                                     FontSize="18"
                                     BorderThickness="0"
                                     Padding="12,6"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Fehérje / 100g"
                                       FontWeight="SemiBold"
                                       Foreground="#FF4B3A2E"/>

                            <TextBox Text="{Binding EditedProtein, Mode=TwoWay}"
                                     PlaceholderText="pl. 13"
                                     FontSize="18"
                                     BorderThickness="0"
                                     Padding="12,6"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal"
                                    Spacing="12"
                                    VerticalAlignment="Center">

                            <TextBlock Text="Miben mérhető az összetevő?"
                                       FontWeight="SemiBold"
                                       Foreground="#FF4B3A2E"
                                       Margin="0,5,0,0"/>

                            <ComboBox x:Name="MeasurementComboBox"
                                      Width="140"
                                      VerticalAlignment="Center"
                                      Margin="10,0,0,0"
                                      ItemsSource="{Binding Units}"
                                      DisplayMemberPath="Label"
                                      SelectedValuePath="Value"
                                      SelectedValue="{Binding EditedMeasurementUnit, Mode=TwoWay}"
                                      Loaded="MeasurementComboBox_Loaded"/>

                        </StackPanel>

                        <StackPanel Margin="0,0,0,-10">
                            <TextBlock Text="Mérhető darabban?"
                                       Foreground="#FF4B3A2E"
                                       FontWeight="SemiBold"/>

                            <ToggleSwitch IsOn="{Binding EditedCanBeMeasuredInPiece, Mode=TwoWay}"
                                          OffContent="Nem"
                                          OnContent="Igen"/>
                        </StackPanel>

                        <Grid Visibility="{Binding EditedCanBeMeasuredInPiece, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Darab súlya (g)"
                                       Foreground="#FF4B3A2E"
                                       FontWeight="SemiBold"/>

                            <TextBox Grid.Row="1"
                                     Text="{Binding EditedOnePieceWeigh, Mode=TwoWay}"
                                     PlaceholderText="pl. 47"
                                     FontSize="18"
                                     BorderThickness="0"
                                     Padding="12,6"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    Spacing="20">

                            <Button Content="Mégse"
                                    Width="120"
                                    Padding="12,6"
                                    CornerRadius="6"
                                    Click="Cancel_Click"/>

                            <Button Content="Hozzáadás"
                                    Width="120"
                                    Padding="12,6"
                                    Background="#FFE5C99F"
                                    Foreground="#FF4B3A2E"
                                    CornerRadius="6"
                                    Click="Save_Click"/>
                        </StackPanel>

                    </StackPanel>
                </Border>

            </Grid>
        </ScrollViewer>

        <controls:ConfirmPopup x:Name="Popup"/>

    </Grid>
</Page>
